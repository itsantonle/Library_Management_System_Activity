//PSUEDO CODE

// import pool 'somewhere'
//import express from express
// const app = express()

// const getQuantity = async () => {
//     return await pool.query("SELECT quantity from BOOK WHERE isbn = ?")
// }
// app.post('/endpoint', await (req,res) => {
//     const quantity = await getQuantity()
//     if(quantity = 0){ throw Error('Cannot borrow Book as the quantity is zero')}
//     else { pool.query("INSER INTO BookLoan")}

// res.status(201).json({success: true})
// })
